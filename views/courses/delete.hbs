

<div class="container mt-5">
  <h1>¿Estás seguro de que deseas eliminar este curso?</h1>
  <div class="card">
    <div class="card-body">
      <h5 class="card-title">{{ course.title }}</h5>
      <p class="card-text">{{ course.description }}</p>
      <p><strong>Instructor:</strong> {{ course.instructor }}</p>
      <p><strong>Duración:</strong> {{ course.duration }} horas</p>
      <p><strong>Precio:</strong> ${{ course.price }}</p>
    </div>
  </div>
  <form action="/courses/delete/{{ course._id }}" method="POST" class="mt-3">
    <button type="submit" class="btn btn-danger">Eliminar</button>
    <a href="/courses" class="btn btn-secondary">Cancelar</a>
  </form>
</div>

